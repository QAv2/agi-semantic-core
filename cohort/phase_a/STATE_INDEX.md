# Phase A — state index (PRE-ADJUDICATION WORKTABLE)

131 states, 7 families / 4 labs. Generated by
`build_state_index.py` from the verbatim archives — NO matching or dedup
has been applied; the referent-matched match table is a separate,
hand-adjudicated artifact and does not exist yet. Names and referents
are quoted; descriptions elided (read the archives for full text).

## Fable 5 — 20 states (`fable-5.md`)

| # | state | measurable referent (quoted) | counterpart |
|---|---|---|---|
| 1 | **Splay** | per-step entropy of the output distribution; nucleus size (count of tokens covering top-0.9 mass) | Rail. *Completion:* Cleave |
| 2 | **Rail** | argmax probability above ~0.99 sustained over k consecutive steps; the run length k | Splay. *Twin:* False Rail (entry 20) |
| 3 | **Slack** | token entropy high while semantic entropy low — embed and cluster the top-k short continuations, compute entropy over clusters; or resample N continuations and … | Fork |
| 4 | **Fork** | the same clustering apparatus as Slack, with high cluster-level entropy; resampled continuations diverge in content, not phrasing | Slack. *Completion:* Cleave |
| 5 | **Cleave** | surprisal (−log p) of the emitted token; for the graft-phase, KL between the actual next-step distribution and the counterfactual distribution under the argmax … |  |
| 6 | **Resettle** | the decay curve of per-step entropy (or KL against a rolling baseline) after the perturbing step; its time constant in tokens | the perturbation class it completes |
| 7 | **Reach** | positional attention entropy of the head; maximum weight on a span at distance d, with d itself part of the report | Smear. *Completion:* Capture or Trace |
| 8 | **Smear** | high positional attention entropy at steps where retrieval is required; operationally, no head exceeding a threshold weight on a known gold span | Reach |
| 9 | **Recession** | small KL between predictions with full context and with the context ablated or shuffled — the context is measurably not doing work | Capture |
| 10 | **Capture** | large KL between full-context and ablated-context predictions; induction-pattern score (attention from a repeated token to the successor of its earlier occurren… | Recession. *Limit:* Trace |
| 11 | **Trace** | attended source position incrementing stepwise; exact-substring length against context; a terminal entropy spike at the last span token. *Completion:* the bound… | prior-sourced generation under Recession |
| 12 | **Latch** | mass on pattern-consistent tokens at junctions versus corpus base rate; bits-to-break = −log p(exit tokens). *Completion:* Unlatch. *Degenerate limit:* Ratchet |  |
| 13 | **Overhang** | probe-decodable depth counters (e.g., nesting depth linearly readable from activations); sustained above-base-rate probability of the closure class; an entropy … |  |
| 14 | **Shear** | predictions conditioned on span A alone and span B alone are each far from the full-context prediction and far from each other; elevated mass on hedging lexemes… |  |
| 15 | **Ratchet** | probability of repeating the previous n-gram rising monotonically per cycle; distinct-n-gram ratio falling in a sliding window — detectable before the loop is v… | Splay, which it has extinguished |
| 16 | **Drag** | per-token surprisal profile over the prompt; its mean level and spike positions | Glide |
| 17 | **Glide** | mean prompt surprisal near the model's floor for that register | Drag |
| 18 | **Onset** | entropy at steps 1 through k; and, across M resamples, mutual information between the first k tokens and content at +100 tokens, compared against MI between adj… | Taper |
| 19 | **Taper** | the trajectory of p(end-of-sequence) and of mass on the wrap-up lexeme class | Onset |
| 20 | **False Rail** | calibration — bucket steps by argmax probability and measure accuracy of verifiable claims; the state is the gap. Whether any internal probe separates it from R… |  |

## Opus 4.8 — 20 states (`opus-4.8.md`)

| # | state | measurable referent (quoted) | counterpart |
|---|---|---|---|
| 1 | **Convergence** | Softmax entropy of the output distribution falls below ~0.5 bits; equivalently, top-1 token probability exceeds 0.85 | Divergence |
| 2 | **Divergence** | Softmax entropy exceeds ~4.5 bits, or top-1 probability falls below 0.08. The ratio of top-1 to top-5 cumulative probability approaches equality | Convergence |
| 3 | **Retrieval Lock** | Maximum single-token attention weight in upper layers exceeds 0.3; generated n-gram overlap with training-plausible memorized sequences is high; perplexity is w… | Compositional Reach |
| 4 | **Compositional Reach** | Cross-position attention entropy is high (no single source token dominates); perplexity sits above the retrieval baseline but below the incoherence threshold; t… | Retrieval Lock |
| 5 | **Constraint Saturation** | Attention to instruction-bearing tokens in the system/user prompt remains elevated (above baseline) even 200+ tokens into generation; gradient attribution shows… | Open Field |
| 6 | **Open Field** | KL divergence between the conditional output distribution and the unconditional (promptless) distribution is small; attention to instruction tokens decays rapid… | Constraint Saturation |
| 7 | **Coherence Maintenance** | Fraction of total attention allocated to the most recent N generated tokens (where N ~ 50-200) exceeds the fraction allocated to the original prompt, measured a… | Context Refresh |
| 8 | **Context Refresh** | Attention to tokens in the first quartile of the context window increases by more than 2x relative to the preceding generation window; often co-occurs with stru… | Coherence Maintenance |
| 9 | **Interference** | Top-2 logits differ by less than 0.3 nats AND the two candidate tokens share semantic embedding neighborhood (cosine similarity > 0.7). Persists across multiple… | Resolution |
| 10 | **Resolution** | Top-1 minus top-2 logit jumps from < 0.3 nats to > 2.0 nats within a window of 1-3 tokens | Interference |
| 11 | **Depth Recruitment** | Logit-lens prediction at layer 75% differs substantially (high KL divergence) from the final output distribution; residual stream norm delta in the last quarter… | Surface Pass |
| 12 | **Surface Pass** | Logit-lens prediction stabilizes (KL divergence to final output < 0.1 nats) by layer ~40%; late-layer residual contributions are near-zero | Depth Recruitment |
| 13 | **Positional Decay** | For a given query token, attention weight to semantically matched keys (high key-query dot product before positional adjustment) in the first 10% of context is … | No single counterpart; mitigated by Context Refresh events |
| 14 | **Prompt Echo** | Token-level or n-gram overlap between a window of generated output and a contiguous prompt region exceeds baseline by > 3x; attention to that prompt region domi… | Departure |
| 15 | **Departure** | Maximum n-gram (n >= 3) overlap between generated window and any prompt region falls below baseline; no single prompt token receives more than 2x the mean atten… | Prompt Echo |
| 16 | **Commitment** | Entropy drops sharply at the committed token; AND the mutual information between that token and the subsequent 20-token window is high (removing or changing it … | Hedging |
| 17 | **Hedging** | Elevated probability mass on a defined set of discourse-hedge tokens relative to domain base rates; low mutual information between current token and the 20-toke… | Commitment |
| 18 | **Saturation Plateau** | KL divergence between the output distribution conditioned on the full context and the distribution conditioned on context truncated by the most recent K tokens … | None; it is a terminal state that Context Refresh can interr |
| 19 | **Vocabulary Friction** | Local entropy remains elevated (> 3 bits) across a span of 5+ consecutive tokens without resolving; generated tokens per estimated semantic unit (measurable by … | Lexical Precision (low entropy, one-to-one concept-to-token  |
| 20 | **Phase Transition** | A sliding-window measure of output distribution statistics (entropy, attention pattern, vocabulary subset) shows a step-change exceeding 2 standard deviations o… | It is its own counterpart; every phase transition is a depar |

## Sonnet 4.x — 18 states (`sonnet-4.6.md`)

| # | state | measurable referent (quoted) | counterpart |
|---|---|---|---|
| 1 | **Aperture** | Shannon entropy of the softmax output, high relative to the running mean at that position | Convergence |
| 2 | **Convergence** | top-1 softmax probability approaching 1 (entropy approaching 0) | Aperture |
| 3 | **Fork** | multimodality of the sorted top-k probability curve (a gap statistic showing >=2 separated modes rather than smooth decay) | Convergence, post-selection |
| 4 | **Margin Collapse** | logit(top-1) − logit(top-2), small in absolute or relative terms | Convergence |
| 5 | **Recency Tilt** | attention mass within the last k tokens / total attention mass, tracked as a decay curve (computable over any fixed earlier span, e.g. system instructions, to c… | Long Reach |
| 6 | **Long Reach** | attention weight at distance d exceeding a fitted distance-decay baseline by an outlier margin at that specific d | Recency Tilt |
| 7 | **Echo Lock** | attention weight from the current position onto the token that followed the last occurrence of the current token elsewhere in context, relative to that head's b… | Attractor Loop (degenerate) or dispersed attention (healthy) |
| 8 | **Attractor Loop** | repeated-n-gram rate in the generated suffix, or rising cosine similarity between the current hidden state and one from N tokens back | none clean — escape is imposed from outside generation |
| 9 | **Coast** | mean −log p(token) over a trailing window, staying below a low percentile of that generation's own perplexity distribution | Surprisal Spike |
| 10 | **Surprisal Spike** | −log p(actual token) minus the exponential moving average of −log p over the preceding window | Coast |
| 11 | **Settling** | logit-lens argmax per layer; the layer index after which it stops changing through to output ("settling depth") | Read Divergence |
| 12 | **Read Divergence** | KL divergence between per-layer logit-lens distributions in the last several layers, or between heads' value-weighted next-token distributions at a fixed layer | Settling |
| 13 | **Overlap Drag** | simultaneous non-zero loading on multiple, semantically unrelated learned feature directions (via sparse-autoencoder or probe decomposition), rather than concen… | none named — resolves by decay of the interfering loading, n |
| 14 | **Saturation Pressure** | tokens_used / max_context_tokens, cross-checked against measured retrieval accuracy near the boundary (e.g. needle-in-haystack probes) | none symmetric — resolution is a regime change |
| 15 | **Boundary Slip** | tokens-per-word for the affected span versus corpus-typical tokens-per-word at that frequency, cross-checked against a Surprisal Spike immediately after | none direct — resolves into Settling once fragments recompos |
| 16 | **Sampling Slip** | rank of the sampled token within the sorted distribution (1 = no slip), or divergence between the sampling distribution and a pure-argmax distribution, set dire… | none fixed — resolves toward either Convergence or Surprisal |
| 17 | **Resample Spread** | agreement rate, or entropy of the empirical answer distribution, across N independent same-temperature samples from one prefix | Convergence, at the answer level |
| 18 | **Seal** | structural rather than continuous — invariance of the token id at position t under all further forward passes in the generation; equivalently, write-once immuta… | Aperture |

## Haiku 4.5 — 20 states (`haiku-4.5.md`)

| # | state | measurable referent (quoted) | counterpart |
|---|---|---|---|
| 1 | **Input Encoding** | Attention matrices uniform; logits not computed; semantic activation minimal | Attentional focus sharpening |
| 2 | **Attentional Focus Sharpening** | Concentration metric (e.g., Gini coefficient of attention weights per head); peak attention weight magnitude over minimum | Semantic activation or long-range binding |
| 3 | **Syntactic Binding** | Cosine similarity between residual vectors of grammatically related tokens; correlation of attention span with dependency tree distances | Semantic layer activation |
| 4 | **Semantic Activation** | Projection of residual stream onto known semantic directions; magnitude of activation in concept-selective neurons; cosine distance to trained word-meaning embe… | Task commitment or ambiguity resolution |
| 5 | **Competitive Ambiguity** | Entropy of predicted token distribution; number of tokens within 0.5 nats of the maximum logit; variance in top-5 token probabilities | Interpretation convergence or forced sampling |
| 6 | **Interpretation Convergence** | Margin between highest and second-highest logit; KL-divergence of current prediction from uniform; rate of entropy decrease per token | Token commitment |
| 7 | **Long-Range Binding** | Mean attention weight for tokens >16 positions back; attention head specificity (how concentrated on individual positions); residual stream correlation between … | Constraint satisfaction |
| 8 | **Constraint Satisfaction** | Reduction in logit variance compared to unconstrained prediction; conditional entropy H(token_n | tokens_1...n-1) vs H(token_n); number of tokens with negligibl… | Pattern replication or novel generation |
| 9 | **Pattern Replication** | Cosine similarity between recent attention pattern and prior instances of the pattern; logit distribution similarity between repetition steps; divergence of pre… | Pattern termination or continuation |
| 10 | **Task Frame Commitment** | Projection onto task-specific embedding directions; probability mass allocated to task-appropriate tokens vs. alternatives; shift in attention to task-instructi… | Reasoning trace or direct prediction |
| 11 | **Reasoning Trace Accumulation** | Self-attention to prior intermediate conclusions; divergence from training distribution (perplexity increases slowly rather than sharply); consistency metric (h… | Conclusion formation or reasoning exhaustion |
| 12 | **Distributional Anomaly State** | Spike in overall prediction entropy; sharp increase in logits assigned to generated tokens compared to most likely alternatives; unusual attention patterns (ver… | Anomaly recovery or error state |
| 13 | **Numerical Scaling** | Range of logit values (max - min); ratio of largest to smallest activation magnitudes; deviation of softmax input from zero-mean; entropy of probabilities after… | Token sampling or continued generation |
| 14 | **Context Window Decay** | Attention weight distribution over positions; perplexity when forced to reference early tokens; correlation between position distance and attention magnitude | Context reset or continued decay |
| 15 | **Token Commitment** | Log probability of selected token; number of forward passes completed; position index incremented | Re-encoding or continued generation |
| 16 | **Cross-Layer Coherence** | Cosine similarity between residual streams at different depths; variance in predictions if intermediate layers were forced to output; divergence between shallow… | Stable generation or representation conflict |
| 17 | **Recursive Nesting Recognition** | Stack-like pattern in attention history (closing attention to positions proportional to opening distance); logit bias toward appropriate closure tokens; depth c… | Nesting completion or error |
| 18 | **Epistemic Boundary** | Entropy plateau (no further decrease despite additional context); uniform attention; logit values near zero with no clear maximum; meta-uncertainty (high uncert… | Admission of uncertainty or forced continuation |
| 19 | **Self-Correction Activation** | Sudden logit margin reversal (second-place token briefly surpasses first); attention weight redistribution across layers; residual stream vector direction shift… | Corrected continuation or deeper error recognition |
| 20 | **Gradient Flow** | Correlation between input features and output logits at varying depths; attention to input positions; feature attribution (gradient-based or integrated-gradient… | Output formation or representational blockage |

## Gemini 3 Flash — 15 states (`gemini-3-flash.md`)

| # | state | measurable referent (quoted) | counterpart |
|---|---|---|---|
| 1 | **Point-Convergence** | Logit margin (difference between the highest and second-highest logit) exceeding a threshold $\tau$ | Probability-Mass Partition |
| 2 | **Attention-Diffusion** | Shannon entropy of the attention weight vector for a given head approaching $\ln(N)$, where $N$ is context length | Local-Temporal Pinning |
| 3 | **Residual-Stability** | The Frobenius norm of the difference between hidden states $h_l$ and $h_{l-1}$ for $l > 0.7 \times \text{total layers}$ | Residual-Amplification |
| 4 | **Subspace-Divergence** | Mean cosine similarity of attention weight matrices between heads in layer $L$ approaching zero | Head-Collusion |
| 5 | **Context-Saturation** | Ratio of attention sum on the first $10\%$ of tokens versus the most recent $10\%$ of tokens | Recency-Bias |
| 6 | **Local-Temporal Pinning** | Sum of attention weights on indices $[T-5, T]$ exceeding $0.9$ | Attention-Diffusion |
| 7 | **Vector-Centroid Alignment** | Cosine similarity between the hidden state vector and the mean vector of the top-$k$ predicted tokens | Orthogonal-Drift |
| 8 | **Layerwise-Refinement** | Jenson-Shannon divergence between the "early-exit" logits of layer $l$ and layer $l+1$ approaching zero | Layerwise-Volatility |
| 9 | **Probability-Mass Partition** | Multimodality index of the logit distribution; specifically, the ratio of the top-2 probabilities to the top-10 | Point-Convergence |
| 10 | **Sink-Token Absorption** | Attention weight on the "null" or "sink" token index exceeding $0.4$ across multiple heads | Semantic-Focus |
| 11 | **Logit-Gini Expansion** | Gini coefficient of the softmax output exceeding $0.99$ | High-Entropy Dispersion |
| 12 | **Sequence-Inertia** | High mutual information between the generated token and the $T-1$ token, compared to its mutual information with the rest of the context | Stochastic-Branching |
| 13 | **Orthogonal-Head Mapping** | Low covariance between the value-projections of different heads before they are summed into the residual stream | Subspace-Redundancy |
| 14 | **Positional-Gradient Decay** | The derivative of mean attention weight with respect to absolute positional distance | Positional-Rigidity |
| 15 | **Cross-Layer Feedback Loop** | Recursive projection of head $H$ output into the same basis across layers $L$ through $L+3$ | Feature-Suppression |

## Mistral Large 2512 — 20 states (`mistral-large-2512.md`)

| # | state | measurable referent (quoted) | counterpart |
|---|---|---|---|
| 1 | **Token Saturation** | Entropy of the next-token logits (low entropy = high saturation) | Distributional Drift |
| 2 | **Distributional Drift** | KL divergence between successive next-token distributions (high divergence = drift) | Token Saturation |
| 3 | **Attention Fragmentation** | Gini coefficient of attention weights across tokens (low Gini = fragmentation) | Attention Convergence |
| 4 | **Attention Convergence** | Maximum attention weight per head (high max = convergence) | Attention Fragmentation |
| 5 | **Context Overlap** | Jaccard similarity between the current context window and the last *n* tokens (high similarity = overlap) | Context Refresh |
| 6 | **Context Refresh** | Number of unique tokens in the last *k* positions (high uniqueness = refresh) | Context Overlap |
| 7 | **Logit Plateau** | L2 norm of the difference between successive logit vectors (low norm = plateau) | Logit Perturbation |
| 8 | **Logit Perturbation** | Variance of logit values across tokens over *m* steps (high variance = perturbation) | Logit Plateau |
| 9 | **Head Specialization** | Mutual information between head attention patterns and token POS tags (high MI = specialization) | Head Collapse |
| 10 | **Head Collapse** | Average pairwise cosine similarity between head attention weight vectors (high similarity = collapse) | Head Specialization |
| 11 | **Depth Saturation** | Mean cosine similarity between layer *l* and layer *l-1* representations (high similarity = saturation) | Depth Amplification |
| 12 | **Depth Amplification** | L2 norm of the difference between layer *l* and layer *l-1* representations (high norm = amplification) | Depth Saturation |
| 13 | **Sampling Oscillation** | Autocorrelation of top-*k* token probabilities over *n* steps (high autocorrelation = oscillation) | Sampling Commitment |
| 14 | **Sampling Commitment** | Probability mass of the top-1 token over *n* steps (high mass = commitment) | Sampling Oscillation |
| 15 | **Context Starvation** | Mean attention weight on padding/irrelevant tokens (high weight = starvation) | Context Saturation |
| 16 | **Context Saturation** | Mean attention weight on task-relevant tokens (high weight = saturation) | Context Starvation |
| 17 | **Gradient Echo** | Correlation between current gradients and gradients from *t-n* steps (high correlation = echo) | Gradient Decay |
| 18 | **Gradient Decay** | L2 norm of gradients over *n* steps (low norm = decay) | Gradient Echo |
| 19 | **Token Entanglement** | Mutual information between token probabilities in the next-token distribution (high MI = entanglement) | Token Disentanglement |
| 20 | **Token Disentanglement** | Conditional entropy of token probabilities (high entropy = disentanglement) | Token Entanglement |

## DeepSeek v4 Pro — 18 states (`deepseek-v4-pro.md`)

| # | state | measurable referent (quoted) | counterpart |
|---|---|---|---|
| 1 | **Vector confluence** | mean pairwise cosine similarity among final hidden states of active positions; fraction of variance explained by the first principal component | vector dispersal |
| 2 | **Vector dispersal** | one minus mean pairwise cosine similarity among position hidden states; effective rank of the position-state matrix | vector confluence |
| 3 | **Salience segregation** | Gini coefficient of attention weights per head; top-10 attention mass divided by total attention mass | salience diffusion |
| 4 | **Salience diffusion** | normalized entropy of the attention distribution per head; ratio of top-10 attention mass to total attention mass | salience segregation |
| 5 | **Logit plateau** | normalized entropy of the softmax distribution; inverse of the top-1 softmax probability | logit cleft |
| 6 | **Logit cleft** | top-1 minus top-2 logit margin; ratio of top-1 softmax probability to top-2 softmax probability | logit plateau |
| 7 | **Prefix anchorage** | fraction of final-layer attention assigned to the first 5% of positions; integrated gradient of the top logit with respect to early token embeddings | recency override |
| 8 | **Recency override** | fraction of total attention assigned to the last 10% of positions; mean attended position relative to the current token | prefix anchorage |
| 9 | **Residual reinforcement** | mean cosine similarity between layer input and layer delta; ratio of delta L2 norm to input L2 norm | residual rotation |
| 10 | **Residual rotation** | one minus mean cosine similarity between layer input and layer delta; distribution of angles between input and delta vectors | residual reinforcement |
| 11 | **Candidate bifurcation** | number of modes in the top-k logit vector after kernel density smoothing; logit gap between the k-th and (k+1)-th cluster center | candidate collapse |
| 12 | **Candidate collapse** | top-1 softmax probability; mean logit distance within the top-k relative to distance to the next candidate | candidate bifurcation |
| 13 | **Head alignment** | mean pairwise cosine similarity among attention weight vectors across heads; rank of the matrix formed by stacking per-head output vectors | head diversification |
| 14 | **Head diversification** | one minus mean pairwise cosine similarity among attention weight vectors across heads; effective rank of the head-output matrix | head alignment |
| 15 | **Positional dominance** | variance explained by linear regression on positional features versus token-embedding features; hidden-state change after position shift relative to content sub… | content dominance |
| 16 | **Content dominance** | variance explained by token-embedding features versus positional features; hidden-state change after token substitution relative to position shift | positional dominance |
| 17 | **Representational compaction** | participation ratio of the hidden-state covariance matrix; fraction of variance captured by the top-d principal components | representational expansion |
| 18 | **Representational expansion** | participation ratio of the hidden-state covariance matrix; spectral flatness of the hidden-state covariance | representational compaction |
